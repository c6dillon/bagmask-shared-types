export interface Payment {
  paymentType: string;
  corePostingCost: number;
  priorityTypeSelected: string;
  priorityTypeCost: number;
  paymentPlanSelected: string;
  paymentPlanCost: number;
  totalPostingCost: number;
}
